name: govee2mqtt
services:
  govee2mqtt:
    image: ghcr.io/wez/govee2mqtt:latest
    container_name: govee2mqtt
    restart: unless-stopped
    env_file:
      - .env
    network_mode: host
    volumes:
      - '/media/hdd1/docker/govee2mqtt/data:/data'
